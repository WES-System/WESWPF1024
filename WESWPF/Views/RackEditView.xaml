<Window x:Class="WES.Views.RackEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WES.Views"
        mc:Ignorable="d"
        xmlns:conv="clr-namespace:WES.Converters"
        Title="{Binding IsAddMode, Converter={StaticResource BoolToTitleConverter}}" 
        Height="300" 
        Width="400"
        Background="Transparent"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None">
    <Border Background="White" CornerRadius="3" BorderThickness="1" BorderBrush="LightGray">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Border CornerRadius="3 3 0 0" Background="#FF2A7491" MouseLeftButtonDown="WinMove">
                <Grid>
                    <TextBlock Text="提示" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="14" Foreground="White" Margin="8"/>
                    <Button ToolTip="关闭" HorizontalAlignment="Right" Margin="8" Style="{StaticResource WindowControlBtnStyle}" Height="24" Width="24" Tag="PowerOff" Click="CloseBtnClick"/>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="10 20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="10"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" Grid.Row="0">
                    <TextBlock Margin="6" Text="编号:" Width="80" VerticalAlignment="Center"/>
                    <TextBox Margin="6" Text="{Binding Rack.ID}" IsReadOnly="True" Width="200"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Grid.Row="1">
                    <TextBlock Margin="6" Text="货架编号:" Width="80" VerticalAlignment="Center"/>
                    <TextBox Margin="6" Text="{Binding Rack.RackCellCode}" Width="200"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Grid.Row="2">
                    <TextBlock Margin="6" Text="行索引:" Width="80" VerticalAlignment="Center"/>
                    <TextBox Margin="6" Text="{Binding Rack.RackCellRowIndex}" Width="200"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Grid.Row="3">
                    <TextBlock Margin="6" Text="列索引:" Width="80" VerticalAlignment="Center"/>
                    <TextBox Margin="6" Text="{Binding Rack.RackCellColumnIndex}" Width="200"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Grid.Row="4" HorizontalAlignment="Center">
                    <Button Content="保存" Command="{Binding SaveCommand}" Width="80" Margin="10"/>
                    <Button Content="取消" Command="{Binding CancelCommand}" Width="80" Margin="10"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
